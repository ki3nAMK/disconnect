<p>Dear {{ name }},</p>

<p>We have received a request to reset the password for your account. If you initiated this request, please use the verification code below to complete the process:</p>

<p><strong>Verification Code:</strong> <code class="code">{{ code }}</code></p>

<p>Note: This code will expire in 2 hours.</p>

<p>If you did not request a password reset, please ignore this email or contact us at for further assistance.</p>

<p>Thank you,</p>
<p>The Support Team</p>

<style>
    .code {
        --ds--code--bg-color: var(--ds-background-neutral, #091E420F);
        font-size: 0.875em;
        font-family: var(--ds-font-family-code, ui-monospace, Menlo, "Segoe UI Mono", "Ubuntu Mono", monospace);
        font-weight: var(--ds-font-weight-regular, 400);
        background-color: var(--ds--code--bg-color, var(--ds-background-neutral, #F4F5F7));
        color: var(--ds-text, #172B4D);
        border-style: none;
        border-radius: var(--ds-border-radius, 3px);
        display: inline;
        padding: 2px 0.5ch;
        -webkit-box-decoration-break: clone;
        box-decoration-break: clone;
        overflow: auto;
        overflow-wrap: break-word;
        white-space: pre-wrap;
    }
</style>